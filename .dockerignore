# Secrets and local config — must never bake into image layers
.env
*.env

# VCS and editor state
.git
.gitignore
.vscode/
.idea/

# Local build artifacts and profiling output
rate-limiter
limiter.test
*.exe
*.prof
*.out
cpu.prof
trace.out

# Tests, load scripts, scratch files (not needed to build the binary)
tests/
*_test.go
k6.js
burst_correctness.js
*.txt

# Build context files that don't belong inside the image
Dockerfile
docker-compose.yml
.dockerignore

# Docs live on GitHub, not in the runtime image
README.MD
